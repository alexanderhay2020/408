function [j_index k_index val] = find_max(array)
temp = size(c);
j = temp(1);
k = temp(2);
max_val = -inf;
for i1 = 1:j
    for 
    if array(i) > max_val;
        max_val = array(i1);
        index = ;;
    end
end
